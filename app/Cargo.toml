[package]
name = "feather-app"
version.workspace = true
edition.workspace = true
publish = false

[[bin]]
name = "feather"
path = "src/main.rs"

[dependencies]
feather-platform.workspace = true
feather-gfx.workspace = true
feather-render.workspace = true
feather-assets.workspace = true
feather-game.workspace = true
bevy_ecs.workspace = true
glam.workspace = true
rapier3d.workspace = true

[dev-dependencies]
# Tests only: constructing PrefabSpec params. The runtime reads them through
# PrefabSpec's typed accessors, so the binary itself needs no JSON dependency.
serde_json = "1"
