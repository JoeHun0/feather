fn main() {
    // Wiring point for the winit loop + ECS world + renderer.
    // The clear-screen bootstrap migrates here (loop + app) and into
    // feather-gfx (device/swapchain) as the next step.
    println!("feather: workspace scaffold OK");
}
